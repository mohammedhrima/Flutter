# ninja_app

A first Flutter app — a single screen for practicing layout widgets (Scaffold, AppBar,
Container, Align). A sandbox for getting a feel for the widget tree.

## Run

```bash
flutter pub get
flutter run
```
