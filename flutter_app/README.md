# flutter_app

A small Flutter app built while learning: a bottom-navigation shell with a Home and a User
page, a dark-mode toggle, and shared state via `ValueNotifier` (`lib/data/notifiers.dart`).
The files in `lib/steps/` (001–008) are the course exercises that led up to it.

## Run

```bash
flutter pub get
flutter run
```
